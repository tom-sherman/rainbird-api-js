# Rainbird API Example

A JS/browser example which simply starts a Rainbird session and logs it to the document.

You can use Parcel to build and view the example in your browser

```
npx parcel index.html
```
